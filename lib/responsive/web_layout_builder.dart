import 'package:flutter/material.dart';

class WebBuilder extends StatelessWidget {
  const WebBuilder({ Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Web')),
    );
  }
}